services:
  pai-admin:
    build:
      dockerfile: Dockerfile
    container_name: pai-admin
    environment:
      - SECRET_KEY=e7486e69d50384e37d2d253a47a904e5146bfcc7a48e62008067ead9c85d548d
      - DATA_DIR=/data
      - DATABASE_URL=sqlite:////database/app.db
      - LOG_TO_STDOUT=true
      - DELETE_FILES_ENABLED=true
    ports:
      - 3000:3000
    volumes:
      - database:/database
      - /Users/jkraemer/Downloads/Kwaai-WG-Data:/data

volumes:
  database:
